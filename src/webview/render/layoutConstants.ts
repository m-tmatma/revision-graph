// Node sizing constants shared between node-size measurement (main.ts) and
// SVG rendering (graphRenderer.ts), so the two stay in sync.

export const NODE_ROW_HEIGHT = 18;
export const NODE_PADDING_X = 12;
export const NODE_PADDING_Y = 4;
export const NODE_MIN_WIDTH = 48;
